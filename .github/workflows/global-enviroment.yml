
name: global-enviroment
env:
  VERSION: $(Get-Content version)

on:
  workflow_dispatch:

jobs:
  print:
    runs-on: [ ubuntu-latest ]
    steps:
    - name: print env
      run: |
        echo $VERSION